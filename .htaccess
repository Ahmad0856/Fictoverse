# FictoVerse - Clean URL Configuration
# This file enables clean URLs for character pages

# Enable Rewrite Engine
RewriteEngine On

# Redirect old character.html?id= format to new clean URLs
RewriteCond %{QUERY_STRING} ^id=([^&]+)$
RewriteRule ^character\.html$ /character/%1? [R=301,L]

# Handle clean character URLs - serve character.html with slug as query parameter
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^character/([^/]+)/?$ character.html?slug=$1 [L,QSA]

# Optional: Redirect trailing slashes
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)/$ /$1 [R=301,L]





